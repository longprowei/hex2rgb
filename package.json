{
  "name": "hex2rgb",
  "version": "0.1.0",
  "description": "A page convert hex color to rgb color or reverse",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r dist",
    "build": "mkdir dist",
    "watch": "watch 'npm run build' ./src"
  },
  "keywords": [
    "hex",
    "color",
    "convert"
  ],
  "author": "Chenglong Wei",
  "repository": {
    "type": "git",
    "url": "https://github.com/longprowei/hex2rgb.git"
  },
  "license": "ISC",
  "devDependencies": {
    "jshint": "^2.9.4",
    "uglify-js": "^3.0.23",
    "watch": "^1.0.2"
  }
}
